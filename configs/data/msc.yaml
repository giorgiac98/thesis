problem: msc
problem_spec:
  use_tanh: False
params:
  obs_norm: True
  seed: 4
  num_prods: 5
  num_sets: 25
  num_instances: 1000
  test_split: 0.3
  data_path: ${hydra:runtime.cwd}/envs/msc_data/${.num_prods}x${.num_sets}/linear/${.num_instances}-instances/seed-${.seed}